export const environment = {
  production: true,
  backendServerUrl: 'http://localhost:8080' //JavaScript is running in client's browser so to access server we must provide server URL accessible from browser
  // backendServerUrl: 'http://spring-boot-ecommerce:8080'
};
